{
  "name": "ytm-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pip install yt-dlp && curl -fsSL https://deno.land/install.sh | sh",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ytmusic-api": "^5.3.0"
  }
}
